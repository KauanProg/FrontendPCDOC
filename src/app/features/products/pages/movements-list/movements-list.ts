import { CommonModule } from '@angular/common'; import { Component, inject } from '@angular/core'; import { FormsModule } from '@angular/forms'; import { ProductsFacade } from '../../facade/products.facade';
@Component({standalone:true,imports:[CommonModule,FormsModule],templateUrl:'./movements-list.html',styleUrl:'./movements-list.css'}) export class MovementsPage { facade=inject(ProductsFacade); selectedProduct=''; movementType='ENTRADA'; quantity=1; showModal=false; constructor(){this.facade.findAll();this.load()} load(){if(this.selectedProduct)this.facade.loadHistory(this.selectedProduct)} productName(id:string){return this.facade.products().find(p=>p.id===id)?.name??id} totalStock(){return this.facade.products().reduce((sum,p)=>sum+p.quantity,0)} openMovement(){this.showModal=true} submit(){if(!this.selectedProduct)return;this.facade.movement(this.selectedProduct,this.movementType,this.quantity).subscribe(()=>{this.showModal=false;this.facade.findAll();this.load()})} }
